---
description: Build the application
allowed-tools: Bash(cargo tauri *), Bash(pnpm *)
---

# Build

## Instructions

1. Development build: `cargo tauri dev`
2. Production build: `cargo tauri build`
3. Frontend only: `pnpm build`
4. Report any build errors with context
