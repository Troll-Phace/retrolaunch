---
description: Run tests with reporting
allowed-tools: Bash(cargo test *), Bash(pnpm test *)
---

# Run Tests

## Instructions

1. Run Rust tests: `cargo test -- --nocapture`
2. Run frontend tests: `pnpm test --run`
3. Report results with pass/fail counts
4. For failures, show the traceback and suggest fixes
